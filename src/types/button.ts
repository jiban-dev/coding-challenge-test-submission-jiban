import { ButtonHTMLAttributes } from 'react';

/*- [ ] Defined a typescript type for the button type inside `src/types/button`. 
This type should only include all the possible types for a button. */

/* Implemeted button type. Added few sample types*/
export type ButtonType = 'button' | 'submit' | 'reset';
export type ButtonVariant = 'primary' | 'secondary';
